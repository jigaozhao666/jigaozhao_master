1.数据发生变化时，怎么更新节点
渲染真实dom开销很大，比如我们更新了一个数据，如果真实渲染dom会引起整个dom的重绘和重排。diff算法可以帮助我们只更新那一部分。
我们可以根据真实dom生成一个 虚拟dom,当虚拟dom某个节点发生改变后，会生成一个新的VNode.
然后vnode 和 oldNode 做对比，发现有不一样的地方，就直接修改在真实dom上，使oldNode 变为 Vnode.
