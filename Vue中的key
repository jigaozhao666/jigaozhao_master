vue中key的作用：

1. 高效渲染dom元素，key的特殊属性主要用于vue虚拟Dom的算法，在新旧nodes对比时，辨识VNodes。

如果不适用key,Vue会使用最大限度减少动态元素并且尽可能尝试修复/再利用相同类型元素的算法。

如果使用key,会基于key的变化重新排列元素顺序，并且会移除key不存在的元素，有相同父元素的子元素必须有自己独特的key

2.管理可复用元素

Vue尽可能高效的渲染元素，通常复用已有元素。而不是重头开始渲染
